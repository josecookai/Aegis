# Railway deployment config for Aegis backend
# Create a new service and link this repo. Set env vars in Railway dashboard.

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
